#utils.py- used to parse inctructions

def parseFile(input):

    if input.startswith('http'):
        
        return None, None
    else:
        return None, None
        # nothing here because I haven't written the code yet
    return
